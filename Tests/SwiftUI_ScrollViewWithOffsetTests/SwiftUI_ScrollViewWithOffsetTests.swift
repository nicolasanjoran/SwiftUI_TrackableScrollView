import XCTest
@testable import SwiftUI_ScrollViewWithOffset

final class SwiftUI_ScrollViewWithOffsetTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SwiftUI_ScrollViewWithOffset().text, "Hello, World!")
    }
}
